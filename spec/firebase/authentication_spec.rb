RSpec.describe Firebase::Authentication do
  xit "has a version number" do
    expect(Firebase::Authentication::VERSION).not_to be nil
  end

  xit "does something useful" do
    expect(true).to eq(true)
  end
end
